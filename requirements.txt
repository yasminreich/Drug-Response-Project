# DIRECT dependencies, for humans. Docker installs requirements.lock instead --
# the full transitive freeze -- because pinning only these 12 left pip free to
# resolve Jupyter's dependency tree to Python-3.11-only releases.
#
# Change a version here, then regenerate the lock:
#   docker build -t drug_response_env . && #   docker run --rm drug_response_env pip freeze > requirements.lock
#
# Results are version-sensitive: scikit-learn, xgboost and torch can all shift the
# reported R2 across releases. Re-pin deliberately, and re-run notebook 03 if you do.
# Python 3.9.25.
pandas==2.3.3
numpy==2.0.2
scipy==1.13.1
scikit-learn==1.6.1
xgboost==2.1.4
torch==2.8.0
matplotlib==3.9.4
seaborn==0.13.2
shap==0.49.1
jupyter==1.1.1
notebook==7.5.7
ipykernel==6.31.0
