# Lightweight test-only dependencies.
# CI runs the unit tests against these instead of the full requirements.txt, which
# would pull ~2 GB of torch to test pure-pandas/sklearn merge and split logic.
pandas==3.0.5
numpy==2.0.2
scipy==1.13.1
scikit-learn==1.6.1
pytest==8.3.4
